"""TSP Heuristic Selector — reproducibility pipeline (data generation, feature
extraction, heuristics, and Stage 1 / Stage 2 models) reconstructed from the
research notebook.

This package is deliberately separate from the ``tsp_heuristics`` distribution
package that already lives under ``src/tsp_heuristics`` in this repository
(the pip-installable ``tsp-predict`` CLI). See the migration report for how
the two relate.
"""
